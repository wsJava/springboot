spring:
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    lettuce:
      pool:
        # 最大连接数, 负值表无限制
        max-active: 8
        # 最大阻塞等待事件, 负值表无限制
        max-wait: -1ms
        # 最大空闲数量
        max-idle: 8
        # 最小空闲连接
        min-idle: 0
#    timeout: 0ms

